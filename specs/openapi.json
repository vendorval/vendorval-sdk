{
  "openapi": "3.0.3",
  "info": {
    "title": "vendorval API",
    "version": "1.0.0",
    "description": "Placeholder. Run `node scripts/sync-openapi.mjs` to populate from the upstream VendorVal API release."
  },
  "security": [{ "bearerAuth": [] }],
  "paths": {},
  "components": {
    "securitySchemes": {
      "bearerAuth": { "type": "http", "scheme": "bearer" }
    }
  }
}
