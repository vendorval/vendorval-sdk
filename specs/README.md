# OpenAPI spec snapshot

`openapi.json` is mirrored from [vendorval-api](https://github.com/Modali-Consulting/vendorval-api). Do not hand-edit it — run `node scripts/sync-openapi.mjs` to refresh from the upstream release.

The `spec-drift.yml` GitHub Action runs this nightly and opens a PR if the snapshot has changed.
